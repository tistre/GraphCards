{% extends "Api/base.html.twig" %}

{% block body_main %}

    <h1>List of node labels</h1>

    <ul>

        {% for nodeLabel in nodeLabels %}

            <li>
                <a href="{{ path('apiAddNode', {'label': nodeLabel}) }}">{{ nodeLabel }}</a>
            </li>

        {% endfor %}

    </ul>

    <a rel="alternate" href="{{ path('apiAddNode') }}">Add a node</a>

{% endblock %}
